{
  "name": "Kick Starter",
  "version": "1.0.0",
  "description": "A full-stack project with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "start:client": "npm run start --prefix client",
    "start:server": "npm run dev --prefix server",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "lucide-react": "^0.474.0"
  }
}
